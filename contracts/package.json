{
  "name": "contracts",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "scripts": {
    "compile": "pnpm compile:links && pnpm compile:event",
    "compile:links": "pnpm compile:_path links Links",
    "compile:event": "pnpm compile:_path event Event",
    "compile:_path": "sh ./scripts/compile-contract.sh"
  }
}
